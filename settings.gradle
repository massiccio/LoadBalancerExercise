plugins {
  id "com.gradle.enterprise" version "3.5.1"
}

gradleEnterprise {
  buildScan {
        termsOfServiceUrl = "https://gradle.com/terms-of-service"
        termsOfServiceAgree = "no"
    }
    buildScan {
        publishOnFailureIf(true == false)
        uploadInBackground = false
    }
}
